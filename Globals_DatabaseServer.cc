#include <IDatabaseServer.h>
#include <DatabaseServer.h>

using namespace StdSystem;

// RequestId IDatabaseService::RequestInfo::_nextid_ = 0;

// std::mutex               DatabaseServer::queueLock;
// std::condition_variable  DatabaseServer::notEmpty;
// std::queue<IDatabaseService::RequestInfo>   DatabaseServer::RequestQueue;
// std::queue <IDatabaseService::Response*>     DatabaseServer::ResponseQueue;
// std::mutex                                  DatabaseServer::ResponseQueueLock;
// std::condition_variable                     DatabaseServer::resQueNotEmpty;